<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>

        <!--
            Database riêng cho test. BẮT BUỘC phải khác DB dev.

            Trước đây hai dòng này là sqlite/:memory: bị comment, nên test chạy
            thẳng vào DB dev — ai gõ `use RefreshDatabase` là xoá sạch dữ liệu
            thật, không cảnh báo gì.

            Không bật lại sqlite được: 38 file migration dùng cú pháp riêng của
            MySQL (ON UPDATE CURRENT_TIMESTAMP, DB::raw, enum, MODIFY COLUMN),
            create_posts_table hỏng ngay ở bảng thứ bảy. Ngoài ra sqlite sẽ làm
            DDL của test lệch khỏi production — đổi bẫy này lấy bẫy khác.

            Tạo một lần:
              CREATE DATABASE news24h_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        -->
        <env name="DB_DATABASE" value="news24h_test"/>

        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
